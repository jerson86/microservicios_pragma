server:
  port:8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jerson86/microservicios_pragma.git
          searchPath: config-data
          userName: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t